import './globals.css';
export const metadata = { title: 'Business Partner — FoodMarket' };
export default function L({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body className="bg-gray-50">{children}</body></html>;
}
