# Frontend droplet — copy to .env before build

# Baked into Next.js at docker build (required)
NEXT_PUBLIC_API_URL=https://api.foodapp.uz/api/v1
NEXT_PUBLIC_APP_URL=https://foodapp.uz

# Middleware JWT verification (same secret as backend)
JWT_SECRET=change-me-same-as-backend
