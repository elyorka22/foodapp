{
  "name": "foodmarket",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-vendor food delivery and grocery marketplace",
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/*",
    "workers"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:api": "npm run dev -w @foodmarket/api",
    "dev:web": "npm run dev -w @foodmarket/web",
    "build": "turbo run build --filter=@foodmarket/api --filter=@foodmarket/database --filter=@foodmarket/web --filter=foodmarket-workers",
    "build:web": "npm run build -w @foodmarket/web",
    "lint": "turbo run lint",
    "db:generate": "npm run generate -w @foodmarket/database",
    "db:migrate": "npm run migrate:dev -w @foodmarket/database",
    "db:push": "npm run push -w @foodmarket/database",
    "db:seed": "npm run seed -w @foodmarket/database",
    "docker:dev": "docker compose -f docker-compose.yml up -d",
    "docker:prod": "docker compose -f docker-compose.prod.yml up -d --build",
    "docker:web": "docker compose -f docker-compose.frontend.yml up -d --build"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
